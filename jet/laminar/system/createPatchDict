FoamFile
{
  version     2.0;
  format      ascii;
  class       dictionary;
  object      createPatchDict;
}

pointSync false;

patches
(
  {
    name front-n-back;

    patchInfo
    {
      type empty;
    }

    constructFrom patches;

    patches (defaultFaces);
  }
);

// vi: set ft=foam et sw=2 ts=2 sts=2:
